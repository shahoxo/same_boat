require "spec_helper"

RSpec.describe SameBoat do
  it "has a version number" do
    expect(SameBoat::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
